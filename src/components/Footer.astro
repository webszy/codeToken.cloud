---
import type { SiteCopy } from "../i18n";

interface Props {
  copy: SiteCopy;
}

const { copy } = Astro.props;
---

<footer class="relative z-10 border-t border-border/20 bg-[rgb(var(--surface-lowest))]">
  <div class="site-container flex min-h-24 flex-col items-center justify-center gap-3 py-8 text-center text-[15px] text-muted sm:flex-row sm:gap-6">
    <p>{copy.footer.copyright}</p>
    <a
      class="inline-grid h-9 w-9 place-items-center rounded-md border border-border/70 text-primary transition hover:border-primary/70 hover:text-foreground"
      href={`mailto:${copy.footer.contactEmail}`}
      aria-label={`Contact ${copy.footer.contactEmail}`}
    >
      <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" aria-hidden="true" xmlns="http://www.w3.org/2000/svg">
        <path d="M4.75 6.75H19.25V17.25H4.75V6.75Z" stroke="currentColor" stroke-width="1.8" stroke-linejoin="round" />
        <path d="M5.25 7.25L12 12.75L18.75 7.25" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" />
      </svg>
    </a>
  </div>
</footer>
